<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/group_members_swipe_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <android.support.v7.widget.RecyclerView android:id="@+id/group_members_recycler_view"
            android:scrollbars="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:background="@android:color/darker_gray"
            xmlns:android="http://schemas.android.com/apk/res/android" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/group_members_add_member_fab"
            android:layout_width="wrap_content"
            app:backgroundTint="@color/yellowish"
            android:tint="@color/colorPrimary"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_height="wrap_content"
            android:foregroundGravity="bottom|right"
            android:layout_marginBottom="90dp"
            android:layout_marginRight="20dp"
            android:onClick="AddUserToGroup"
            app:srcCompat="@android:drawable/ic_input_add" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/group_members_request_fab"
            android:layout_width="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            app:backgroundTint="@color/yellowish"
            android:tint="@color/colorPrimary"
            android:layout_height="wrap_content"
            android:foregroundGravity="bottom|right"
            android:layout_marginBottom="20dp"
            android:layout_marginRight="20dp"
            android:onClick="RequestAccess"
            app:srcCompat="@android:drawable/ic_popup_reminder" />

    </RelativeLayout>

</android.support.v4.widget.SwipeRefreshLayout>
